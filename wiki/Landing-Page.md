# Landing Page

Implemented in `apps/site` as a static Vite app. Keep this page as the compact content brief and update both when positioning changes.

## Hero

Passkey-only auth for Cloudflare apps.

## Subhero

Add WebAuthn signup, login, sessions, and invite codes to Workers + D1 without passwords, magic links, or hosted auth lock-in.

## CTA

```bash
npm install @passkeeper/core @passkeeper/cloudflare @passkeeper/d1
```

## Sections

```txt
1. Why passkey-only?
2. Cloudflare-first quickstart
3. Invite-only signup
4. Bring your own database
5. Tiny API surface
6. Security model
7. Roadmap
```
