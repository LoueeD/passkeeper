{
  "name": "@passkeeper/example-cloudflare-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@passkeeper/cloudflare": "workspace:*",
    "@passkeeper/d1": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
